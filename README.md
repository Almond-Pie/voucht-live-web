# Voucht Live Web

Simple static website for Voucht, built for Netlify hosting.

## Deployment

- `index.html` is the site entry point.
- `netlify.toml` configures Netlify to publish from the repository root.

## Notes

- Update meta tags in `index.html` for SEO.
- All CTAs currently use `mailto:hello@voucht.co.uk`.
- Add a `favicon.png` file to improve browser tab display.
